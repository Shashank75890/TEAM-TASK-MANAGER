# Nixpacks configuration for Spring Boot Maven project
# Railway uses this instead of Dockerfile — no Docker Hub dependency

[phases.build]
cmds = ["mvn clean package -DskipTests -B"]

[start]
cmd = "java -Djava.security.egd=file:/dev/./urandom -jar target/task-manager-0.0.1-SNAPSHOT.jar"
